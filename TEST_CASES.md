# 测试用例

## 测试原则

- 先定义什么叫正确，再写代码。
- 覆盖正常场景、边界场景、异常场景。
- 测试意图必须人工 review，不能只相信自动生成的测试。

## 用例 1：正常场景

输入：

```json
{}
```

期望输出：

```json
{}
```

验证点：

- 核心功能可以正常完成。

## 用例 2：边界场景

输入：

```json
{}
```

期望输出：

```json
{}
```

验证点：

- 边界值处理正确。

## 用例 3：异常场景

输入：

```json
{}
```

期望输出：

- 返回明确错误，或者抛出明确异常。

验证点：

- 非法输入不会导致未知异常。

## 用例 4：业务陷阱

输入：

```json
{}
```

期望输出：

```json
{}
```

验证点：

- 覆盖最容易被 AI 或开发者写错的业务规则。
